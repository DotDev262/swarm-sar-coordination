# swarm-sar-coordination

Hybrid multi-agent drone SAR simulation. Python 3.11 + Pygame + uv.

## Setup

uv sync

## Run

uv run python -m swarm_sar
uv run python -m swarm_sar --sweep --help
uv run python -m swarm_sar --plot <path>

## Test

uv run pytest
